import 'package:flutter/material.dart';

TextStyle linkStyle(final context) => TextStyle(
      color: Theme.of(context).primaryColor,
      decoration: TextDecoration.underline,
      fontWeight: FontWeight.bold,
    );
